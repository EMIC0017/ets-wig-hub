// ETS WIG Hub — analytics endpoint configuration.
// After deploying analytics/apps-script.gs (see SETUP_ANALYTICS.md) paste your /exec URL below.
// While this is empty the tracker is a no-op (no requests sent).
window.WIG_ANALYTICS_ENDPOINT = "";

// Optional: turn off all tracking by setting this to true (e.g., during local dev).
window.WIG_ANALYTICS_DISABLED = false;
